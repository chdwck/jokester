{
  "name": "jokester",
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "export NODE_ENV=development && lerna run --parallel --stream dev",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^7.1.3"
  }
}
